[core]
    remote = minio
['remote "minio"']
    url = s3://image-captioning/dvc-store
    endpointurl = https://s3.minio.lokieee.in
    access_key_id = minioadmin
    secret_access_key = minioadminpassword
    use_ssl = true
    region = us-east-1
