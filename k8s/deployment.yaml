apiVersion: apps/v1
kind: Deployment
metadata:
  name: captioning-api
  labels:
    app: captioning-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: captioning-api
  template:
    metadata:
      labels:
        app: captioning-api
    spec:
      containers:
        - name: captioning-api
          image: your-registry/captioning-api:latest
          ports:
            - containerPort: 8000
          env:
            - name: MLFLOW_TRACKING_URI
              value: "http://mlflow-service:5000"
            - name: MLFLOW_REGISTERED_MODEL_NAME
              value: "image-captioner"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: captioning-api-service
spec:
  selector:
    app: captioning-api
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
